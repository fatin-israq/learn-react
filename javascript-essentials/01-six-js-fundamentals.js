// variable

// / let and const

let name = "Talha";
name = "Fatin Israq"; // Can be changed

const countryName = "BD"; // const can not be changed later

// If else

const num = 20;
if (num >= 20 || num <= 30) {
  console.log("YES");
} else {
  console.log("NO");
}

// Array Methods
const friends = ["Hero", "Joker", "Batman"];

friends.push("Superman"); // add element at the end
console.log(friends);

friends.pop(); // removes last element
console.log(friends);

friends.unshift("Superman"); // add element from first
console.log(friends);

friends.shift("Superman"); // removes first element
console.log(friends);
